
/*
 * These values need to correspond to the domain decomposition specified in POP_DomainSizeMod.F90
 *
 * km = POP_km
 * nt = POP_nt
 * nx_block = POP_blockSizeX + 4
 * ny_block = POP_blockSizeY + 4
 * max_blocks_clinic = POP_maxBlocksClinic
 */

#define km 42
#define nx_block 64
#define ny_block 64
#define nt 2
#define max_blocks_clinic 2

